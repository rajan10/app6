import { ForbibddenNameDirective } from './forbibdden-name.directive';

describe('ForbibddenNameDirective', () => {
  it('should create an instance', () => {
    const directive = new ForbibddenNameDirective();
    expect(directive).toBeTruthy();
  });
});
